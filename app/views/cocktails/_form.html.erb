<%= simple_form_for @cocktail do |f|%>
  <%= f.input :name %>
  <label>
    UPLOAD
    <div class="drop-image">
      Upload an image
    </div>
    <%= f.input :photos, as: :attachinary, label: false %>
  </label>
  <%= f.submit "Add a cocktail", class: "btn btn-danger", :wrapper => :nested %>
<% end %>
